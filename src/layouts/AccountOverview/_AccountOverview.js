import React, { Component } from 'react';
import UserCard from '../../components/UserCard';

export default class _AccountOverview extends Component {
  render() {
    return (
      <div>
        <UserCard />
      </div>
    );
  }
}
